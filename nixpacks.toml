[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.virtualenv"]

[phases.install]
cmds = ["cd Backend && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo \"Build completed\""]

[start]
cmd = "cd Backend && python start_secure.py"
